using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

public class Enums
{
    public enum Action
    {
        LIST = 1,
        SELECT = 2,
        CREATE = 3,
        UPDATE = 4,
        DELETE = 5,
        ReActive = 6,
        FIELDUPDATE = 7
    }

    public enum AppSession
    {
        FullName
    }

  
   
}